# Entry Points
- Python package exposes CLI utilities under `src/good_agent/cli` (invoke via `uv run python -m good_agent.cli.<module>` as needed).
- Agent functionality consumed programmatically via `good_agent.agent.Agent` and related core modules.

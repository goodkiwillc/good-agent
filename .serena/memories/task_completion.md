# Task Completion Checklist
1. Ensure specs in `.spec/` updated if new feature.
2. Apply code changes following existing patterns; avoid duplication.
3. Run `uv run ruff check`, `uv run mypy`, and `uv run pytest`.
4. Report diagnostics in `path:line` format if failures occur.
5. Summarize changes succinctly; avoid altering docs unless requested.

# Suggested Commands
- **Install deps:** `uv sync`
- **Lint:** `uv run ruff check`
- **Format check:** `uv run ruff format --check`
- **Type check:** `uv run mypy` (targeted modules or full project)
- **Tests:** `uv run pytest`
- **Search:** prefer `rg`, `ast-grep` for structural queries.
